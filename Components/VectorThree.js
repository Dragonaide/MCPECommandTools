export function Vector3(x, y, z) {
  x = Number();
  y = Number();
  z = Number();
  // Check if vector properties are numbers
  if (x !== Number()) {
    console.error("Vector3 properties can only be numbers.");
  }
  if (y !== Number()) {
    console.error("Vector3 properties can only be numbers.");
  }
  if (z !== Number()) {
    console.error("Vector3 properties can only be numbers.");
  }
}
